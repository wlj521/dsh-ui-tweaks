/**
 * Official app icons for the 打开项目 menu — JetBrains product marks are
 * the full-color SVGs from resources.jetbrains.com (gradient ids namespaced
 * per icon so several can coexist on one page), VS Code from simple-icons,
 * Explorer a filled Windows-yellow folder. Sizing lives in CSS.
 * @module dsh-ui-tweaks/client/appicons
 */
/** target key -> inline SVG markup (trusted build-time constants). */
export declare const APP_ICONS: {
    explorer: string;
    vscode: string;
    idea: string;
    goland: string;
    webstorm: string;
    pycharm: string;
};
//# sourceMappingURL=appicons.d.ts.map